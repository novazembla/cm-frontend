TODO: 
- Domain: Update FE start scripts (in ./bin and cm_frontend_deploy)
- Domain: Install Cron Job FE start scripts (in ./bin and cm_frontend_deploy)
  
- Upgrade node on prod server
      nvm install 24
      nvm alias default 24
      nvm use 24
      update all helper scrips as they hardcode nvm use 20

- Set CSP Header (adjust domains ...)
<IfModule mod_headers.c>
      Header set Content-Security-Policy "default-src 'self'; \
            img-src 'self' data: bzl-test.hs-example.de bzl-test.hs-map.example.de; \
            script-src 'self' 'unsafe-inline' api.bzl-test.hs-bzl-test.hs-example.de map.bzl-test.hs-example.de; \
            style-src 'self' 'unsafe-inline'; \
            connect-src 'self' api.bzl-test.hs-example.de map.bzl-test.hs-example.de; \
            worker-src 'self' blob:;"
</IfModule>

OPEN 
-    Access Control Origin Header gesetzt mit Wildcard wird u.U. wegen embed-code benötigt, wird geprüft und, falls möglich, geändert

NEARLY DONE: 
-     Ticket fee needs to be shown on event or event detail
-     CSP-Header wird ergänzt (needs to be doployed on prod)

DONE:
-     Update/Restyle placeholder in event suggestion form. 
-     „zurück“-Element kann ergänzt werden, hat aber Design-Implikationen
-     Highlights auf Startseite selbstlöschend im backend nach Ablauf der Veranstaltung
-     Angabe Ticketpreise verbindlich (als Textfeld, um Preisspannen angeben zu können)
      - ticketpreis is in BE optional (kalender import hat keine preise, FE enforced aber die constraint)
-     Inhalte vorschlagen- Umwandlung von Umlauten in den Slugs, z.B. ö → oe
-     Veranstaltungen Desktop 1- statt 2-spaltig 
-     Umstellung der Adresssuche auf OpenStreetMap (funktioniert nicht mehr seit Ende August) 
      INFO: nicht die schnellste läuft aber und ist "gratis"
-     Aktualisierung der Codebasis, Update sämtlicher medium, high und critical Common Vulnerability Scorings (CVCs)
-     Kartenpunkte Mobilansicht 1- statt 2-spaltig (Einrichtungsart, etc.)
-     Struktur (Auszeichnung von Landmarks), hinzufügen von „Aria role Attributen“ (button/navigation)

NO-ACTION NEEDED:
-     Tastaturbedienbarkeit: Kartenpunkte sind über die Auflistung erreichbar, Karteninhalt wird für screenreader durch aria-hidden=“true“ verborgen